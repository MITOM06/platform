server:
  port: 8080

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27018/platform
    redis:
      host: localhost
      port: 6379
  jackson:
    default-property-inclusion: non_null

app:
  jwt:
    secret: ${JWT_ACCESS_SECRET}
