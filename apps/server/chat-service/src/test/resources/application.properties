# Disable tracing during tests — no live OTLP collector is available.
# This prevents the OTLP exporter from logging connection-refused warnings
# and avoids any startup failure caused by tracing auto-config.
management.tracing.enabled=false
management.otlp.tracing.endpoint=http://localhost:4318/v1/traces
